
spring:
  mvc:
    log-request-details: true
management:
  endpoint:
    env:
      show-values: always
    configprops:
      show-values: always
    health:
      show-details: always
      show-components: always
  endpoints:
    web:
      exposure:
        include: '*'
  httpexchanges:
    recording:
      enabled: true

routing:
  fallback:
    invalid-message: "google-storage://loggkamel-dev-fallback-invalid-message"
  postgres:
    consumer: "google-storage://loggkamel-dev-postgresql-consumer?deleteAfterRead=false&exceptionHandler=#gcpConsumerPollingExceptionHandler"
    consumer-delete: "google-storage://loggkamel-dev-postgresql-consumer?operation=deleteObject"
    invalid-message: "google-storage://loggkamel-dev-postgresql-invalid-message"
  loggline:
    bucket: "google-storage://loggkamel-dev-logglines?deleteAfterRead=false&exceptionHandler=#gcpConsumerPollingExceptionHandler"
    bucket-delete: "google-storage://loggkamel-dev-logglines?operation=deleteObject"
    invalid-message: "google-storage://loggkamel-dev-logglines-invalid-message"

debug: false