
spring:
  mvc:
    log-request-details: true
management:
  endpoint:
    env:
      show-values: always
    configprops:
      show-values: always
    health:
      show-details: always
      show-components: always
  endpoints:
    web:
      exposure:
        include: '*'
  httpexchanges:
    recording:
      enabled: true

routing:
  postgres:
    consumer: "google-storage://loggkamel-dev-postgresql-consumer"
    dead-letter: "google-storage://loggkamel-dev-postgresql-deadletter"
    invalid-message: "google-storage://loggkamel-dev-postgresql-invalid-message"
  loggline:
    bucket: "google-storage://loggkamel-dev-logglines"
    dead-letter: "google-storage://loggkamel-dev-logglines-deadletter"
    invalid-message: "google-storage://loggkamel-dev-logglines-invalid-message"

debug: false